/**
 * Configuración de entorno para desarrollo local.
 * Se usa por defecto al ejecutar `ng serve` o `ng build --configuration development`.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:9000',
};
